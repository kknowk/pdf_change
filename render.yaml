services:
  - type: web
    name: pdf-converter
    env: python
    buildCommand: |
      apt-get update && apt-get install -y gcc libmupdf-dev
      pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: development
